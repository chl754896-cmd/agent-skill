# Text Review Workflow

## 1. Workflow 目标

这个 Workflow 用于审核用户输入的一段文本。

它会自动检查文本是否为空，以及文本的表达、语言、冗余和结构是否存在明显问题，最后生成一份简单的审核结果和修改建议。

第一阶段的重点不是做到完全智能，而是先建立一个容易理解、可以逐步改进的自动化流程。

## 2. 输入

用户输入一段需要审核的文本。

例如：一段说明、一封邮件、一篇短文，或一段工作记录。

## 3. Workflow 流程

### 1. 接收用户文本

- 输入：用户提交的原始文本。
- 处理逻辑：保存用户输入，作为后续检查的对象。
- 输出：原始文本。
- 失败时怎么办：如果没有收到输入，提示用户重新输入文本。

### 2. 检查输入是否为空

- 输入：原始文本。
- 处理逻辑：检查文本是否为空，或只包含空格、换行等没有实际内容的字符。
- 输出：有效文本，或输入为空的错误提示。
- 失败时怎么办：停止后续审核，并返回“请输入需要审核的文本”。

### 3. 检查表达是否清晰

- 输入：有效文本。
- 处理逻辑：检查是否存在明显表达含糊、句子不完整或意思难以理解的内容。
- 输出：清晰度相关的问题列表；如果没有发现明显问题，则记录“未发现明显清晰度问题”。
- 失败时怎么办：如果暂时无法判断，记录“本项暂未能准确判断”，但继续进行其他检查。

### 4. 检查明显错别字和语病

- 输入：有效文本。
- 处理逻辑：检查明显错别字、常见用词错误、标点问题和简单语病。
- 输出：发现的语言问题列表；如果没有发现明显问题，则记录“未发现明显错别字或语病”。
- 失败时怎么办：如果无法确定某个问题是否正确，标记为“建议人工确认”，不直接认定为错误。

### 5. 检查内容是否冗余

- 输入：有效文本。
- 处理逻辑：检查是否存在重复句子、重复表达，或内容过长但没有新增信息的部分。
- 输出：冗余问题列表和可精简的建议。
- 失败时怎么办：如果无法确定内容是否冗余，记录“未发现可明确判断的冗余内容”，并继续后续检查。

### 6. 检查结构是否混乱

- 输入：有效文本。
- 处理逻辑：检查段落是否合理、内容顺序是否连贯，以及主题是否频繁跳转。
- 输出：结构问题列表和调整建议。
- 失败时怎么办：如果文本过短，无法判断结构时，记录“文本较短，暂不进行结构判断”。

### 7. 汇总问题并生成审核结果

- 输入：清晰度、语言、冗余和结构检查的结果。
- 处理逻辑：汇总所有发现的问题，并生成总体审核结果和修改建议。
- 输出：完整的文本审核报告。
- 失败时怎么办：如果某一项检查没有结果，在报告中明确说明该项未完成或需要人工确认，其他结果仍然正常输出。

## 4. 最终输出格式

审核报告包含以下三部分：

```text
审核结果：通过 / 建议修改 / 需要较多修改

发现的问题：
- 问题类型：具体说明
- 问题类型：具体说明

修改建议：
- 建议如何修改
- 建议如何修改
```

如果用户没有输入文本，输出：

```text
审核失败：请输入需要审核的文本。
```

## 5. 技术实现规划

第一版：

- 使用最简单的 Python 和规则实现。
- 先完成输入为空检查、基础长度检查、简单重复内容检查，以及固定格式的审核报告输出。
- 每完成一项功能，都进行简单测试和验证。

后续版本：

- 再逐步加入 Codex 或 AI 模型。
- 利用更智能的能力，提高对“表达清晰度”“语病”“结构”等主观问题的判断能力。
- 保持逐步升级：先理解简单规则，再增加更复杂的能力。

## 6. 自动化规划

未来逐步升级为以下流程：

```text
用户输入
  ↓
Python 自动检查
  ↓
生成审核结果
  ↓
自动测试
  ↓
GitHub Actions 自动执行
  ↓
Codex 辅助分析
```

在当前阶段，只先理解和实现最基础的 Python 自动检查，不提前引入不需要的复杂技术。
